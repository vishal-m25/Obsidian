
- At a time only one thread is allowed to execute a python bytecode.
- hence, multi-threading is not directly possible in python.
- so `multiprocessing` library is used to perform multi-threading.
- This library makes a separate memory for the thread to indicate it as a separate program